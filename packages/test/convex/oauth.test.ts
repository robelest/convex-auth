import { test } from "vitest";

// TODO: Re-add OAuth provider tests once we implement a proper
// Google OIDC mock (discovery + token exchange + id_token).
// The previous GitHub OAuth tests were removed because the app
// config switched to Google as the OAuth provider.

test.todo("sign up with oauth");
test.todo("sign in with oauth");
test.todo("redirectTo with oauth");
