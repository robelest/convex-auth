# @robelest/samlify

Internal SAML 2.0 package for the `robelest/convex-auth` monorepo.

This package contains the edge-compatible `samlify` runtime extracted from the
standalone migration workspace and is not published independently.
