{
  "name": "@robelest/samlify",
  "version": "0.0.1-preview.2",
  "files": [
    "dist",
    "README.md"
  ],
  "description": "Edge-compatible SAML 2.0 runtime used by convex-auth",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && vp exec tsdown",
    "typecheck": "vp exec tsc --project tsconfig.json --noEmit",
    "test": "vp test --root ../.. --project samlify",
    "test:once": "vp test --root ../.. --run --project samlify",
    "check:dist-import": "vp exec node ./scripts/check-dist-import.mjs",
    "check:publint": "vp exec publint --strict",
    "check:packaging": "vp run check:publint && vp run check:dist-import",
    "check:runtime-imports": "vp exec node ./scripts/check-forbidden-runtime-imports.mjs",
    "report:edge-gaps": "vp exec node ./scripts/report-edge-runtime-gaps.mjs",
    "prepublishOnly": "vp run build && vp run typecheck && vp run check:packaging"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "camelcase": "^6.3.0",
    "fflate": "^0.8.2",
    "fontoxpath": "^3.33.0",
    "jsrsasign": "^11.1.0",
    "uuid": "^8.3.2",
    "xml-escape": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "publint": "^0.3.17",
    "timekeeper": "^2.3.1",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
