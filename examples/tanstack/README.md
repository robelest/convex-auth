# Convex Auth TanStack Demo

This example app demonstrates `@robelest/convex-auth` with TanStack Start, password sign-in/sign-up, anonymous auth, and a real-time chat UI built with shadcn/ui.

It reads `VITE_CONVEX_URL` from the monorepo root `.env.local`.
